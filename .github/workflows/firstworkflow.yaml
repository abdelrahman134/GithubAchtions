name: first-workflow

on:
    push:
        branches: main
jobs:
    first_lab:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v6.0.2

            - name: list files
              run: ls -ltra

            - name: run shell script
              run: |
               chmod +x HelloAbdelrahman.sh
               ./HelloAbdelrahman.sh